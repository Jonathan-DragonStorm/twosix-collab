name = "http-keyvalue-crud"
language = "tinygo"
type = "component"
version = "0.1.0"

[component]
wit_world = "component"
wasm_target = "wasm32-wasip2"
build_command = "tinygo build -target=./component.json -scheduler=asyncify -gc=conservative -o kv-example.wasm ."
